# Dominios

El monolito modular reserva módulos para `auth`, `audit`, `whatsapp`, `conversations`, `customers`, `catalog`, `orders` y `reporting`. Cada módulo debe exponer una interfaz explícita y evitar dependencias circulares.
